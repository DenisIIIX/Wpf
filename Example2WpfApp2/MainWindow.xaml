<Window x:Class="Example2WpfApp2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Example2WpfApp2"
        mc:Ignorable="d"
        Title="Тестовый редактор" Height="450" Closing="Window_Closing" MinWidth="350" MinHeight="150" Width="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition/>
            <RowDefinition Height="20"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Content="Открыть" Margin="10" Padding="5" Width="70" HorizontalAlignment="Left" Click="OpenClick_Click"/>
            <Button Grid.Column="1" Content="Сохранить как" Margin="10" Padding="5" Width="100" HorizontalAlignment="Left" Click="SaveClick_Click"/>
            <Button Grid.Column="2" Content="О программе" Margin="10" Padding="5" Width="100" HorizontalAlignment="Left" Click="ProgramInfo_Click"/>
        </Grid>
        <TextBox Name="textBox1" 
                 Grid.Row="1" 
                 AcceptsReturn="True" 
                 AcceptsTab="True" 
                 HorizontalScrollBarVisibility="Auto" 
                 VerticalScrollBarVisibility="Auto" KeyDown="textBox1_KeyDown" />
        <StatusBar Grid.Row="2">
            <StatusBarItem>
                <TextBlock x:Name="StatusText" 
                           Text="Готов"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
